fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2

# CATATAN: torch TIDAK ditaruh di sini secara sengaja.
# torch CPU-only diinstall di baris RUN terpisah pada Dockerfile,
# supaya --extra-index-url PyTorch tidak "bocor" ke resolusi paket
# lain (pandas, numpy, dll) yang bisa memaksa pip build dari source
# dan gagal (metadata-generation-failed).

sentence-transformers==3.1.1
# optimum + onnxruntime dibutuhkan untuk backend="onnx" di SentenceTransformer
optimum[onnxruntime]==1.22.0
onnxruntime==1.19.2